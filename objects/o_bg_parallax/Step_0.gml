// Foreground
layer_x(fore_id, lerp(0, camera_get_view_x(view_camera[0]), 0.7));

// Midground
layer_x(mid_id, lerp(0, camera_get_view_x(view_camera[0]), 0.8));

// Background
layer_x(back_id, lerp(0, camera_get_view_x(view_camera[0]), 0.9));


